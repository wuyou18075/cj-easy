# cj-easy 项目文件功能说明

## 保留的核心文件

| 文件 | 主要功能 |
|------|----------|
| [`install.sh`](install.sh) | 主程序入口 `cj`：Nginx、证书、反向代理；并远程调用系统/防火墙/TCP/Docker 子脚本 |
| [`system-tool.sh`](system-tool.sh) | 系统工具菜单（由 `install.sh` 远程调用） |
| [`firewall-tool.sh`](firewall-tool.sh) | 防火墙工具（由 `install.sh` 远程调用） |
| [`tcp.sh`](tcp.sh) | TCP/内核与网卡调优（由 `install.sh` 远程调用；也可 README 独立一键） |
| [`docker-install-dp.sh`](docker-install-dp.sh) | Docker 主面板 `dp`：安装 Docker、容器管理、进入百宝箱 |
| [`docker-tool-install.sh`](docker-tool-install.sh) | 安全百宝箱：拉模板、解析参数、安装/卸载/迁移 |
| [`docker-compose-tools.yml`](docker-compose-tools.yml) | 百宝箱云端应用模板（`#START` / `#NAME` / `#PARAM_*` / `#END`） |
| [`README.md`](README.md) | 项目总览、一键命令、主程序（`install.sh` / `cj`）使用说明 |
| [`LICENSE`](LICENSE) | 许可证 |

## 文档文件

| 文件 | 内容 |
|------|------|
| [`README.md`](README.md) | 主入口文档（含主程序使用说明） |
| [`Docker工具dp使用说明.md`](Docker工具dp使用说明.md) | `dp` 与百宝箱操作 |
| [`百宝箱配置解析.md`](百宝箱配置解析.md) | `docker-compose-tools.yml` 标签与解析规则（新增应用必读） |
| [`项目文件功能说明.md`](项目文件功能说明.md) | 本文件：各文件职责与清理记录 |

## 已删除的无用/过时文件

以下文件**未被** `install.sh` / `docker-install-dp.sh` / `docker-tool-install.sh` 作为正式入口依赖，或仅为占位/草稿/临时目录，已删除：

| 已删路径 | 原因 |
|----------|------|
| `bbr3-install.sh` | 占位空模块，无实际逻辑，也无主流程引用 |
| `docker-Komari.sh` | 旧版 Komari 独立安装器，功能已被百宝箱模板 + `docker-tool-install.sh` 取代 |
| `docker-manager.sh` | 旧版 Docker 管理脚本，现网入口是 `docker-install-dp.sh`（`dp`） |
| `show_tcp_proxy_params.sh` | 独立查看 TCP 参数脚本，主流程未引用 |
| `yml.txt` | 百宝箱语法草稿，已整理进 `百宝箱配置解析.md` |
| `.tmp-sub2/`（含 `install-cpamp.sh`、`docker-compose.local.yml`、`.env.example`） | 本地临时/试验目录，不参与线上入口 |
| `vps-tg-bot-install.sh` | 独立 TG 运维机器人，与 `cj`/`dp`/百宝箱主链路无关，已从 README 移除并删除 |

## 明确保留的「看起来像重复」的文件

| 文件 | 为何保留 |
|------|----------|
| `system-tool.sh` | `install.sh` 的 `REMOTE_SYS_TOOL` |
| `firewall-tool.sh` | `install.sh` 的 `REMOTE_FIREWALL` |
| `tcp.sh` | `install.sh` 的 `REMOTE_TCP`，且 README 独立一键 |

## 运行时产物（不在仓库，在服务器）

| 路径 | 说明 |
|------|------|
| `/usr/local/bin/cj` | 主程序本地副本 |
| `/usr/local/bin/dp` | Docker 面板本地副本 |
| `/app/docker/docker-compose.yml` | 百宝箱安装生成的本机 compose |
| `/app/docker/*` | 各应用挂载数据与备份 |
| `/tmp/remote_docker_tools.yml*` | 模板拉取缓存 |
