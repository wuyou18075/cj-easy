# Docker 百宝箱配置解析说明

本文说明 [`docker-compose-tools.yml`](docker-compose-tools.yml) 的模板语法，以及 [`docker-tool-install.sh`](docker-tool-install.sh) 如何解析、交互、安装。

---

## 1. 文件角色

| 文件 | 作用 |
|------|------|
| `docker-compose-tools.yml` | 云端「应用商店」模板。仓库 `main` 分支上的这份文件会被百宝箱远程拉取。 |
| `docker-tool-install.sh` | 拉取模板 → 解析 `#NAME` / `#PARAM_*` → 交互输入 → 写入本机 compose 并 `up`。 |
| `/app/docker/docker-compose.yml` | 本机实际运行的 compose（安装结果，不是仓库里的模板）。 |
| `/tmp/remote_docker_tools.yml` | 拉取下来的临时模板缓存。 |

远程地址（脚本内）：

- raw: `https://raw.githubusercontent.com/wuyou18075/cj-easy/main/docker-compose-tools.yml`
- GitHub API / jsDelivr 作为备用通道

---

## 2. 块结构总览

文件顶层固定有一行：

```yaml
services:
```

其后是**多个互不干扰的应用块**。每个应用块必须成对出现：

```text
#START
  … 元数据 + compose 服务定义 …
#END
```

**规则：**

- 读到 `#START` → 一个应用块开始
- 读到 `#END` → 该应用块结束
- 中间内容 = 该应用的元数据标签 + 真正的 Docker Compose 服务配置
- 一个 `#START`…`#END` 可以包含**一个或多个**服务（例如 CPA + CPAMP 完整栈）

---

## 3. 元数据标签

### 3.1 `#NAME:`（必填，固定）

```text
#NAME:探针监控komari
```

- 固定前缀：`#NAME:`
- 后面整段字符串 = **面板上显示的名字**
- 大厅菜单用 `grep -oP '^#NAME:\K.*'` 列出全部应用
- 选中后按「名字」在模板里定位对应 `#START`…`#END` 块

注意：名字里可以有空格、`+` 等字符；安装器匹配时用字面量 `index()`，避免正则被特殊字符拆坏。

### 3.2 `#PARAM_xxx:`（可选，可多行）

```text
#PARAM_ADMIN_USERNAME:管理员用户名=admin
#PARAM_ADMIN_PASSWORD:管理员密码=admin
#PARAM_PORT:端口=25774
#PARAM_ADMIN_KEY:CPAMP管理员密钥(登录management.html用)
```

**语法：**

```text
#PARAM_<KEY>:<提示文案>[=默认值]
```

| 部分 | 含义 |
|------|------|
| `PARAM_<KEY>` | 参数名。安装时会替换模板里的 `{PARAM_<KEY>}` |
| `:` 后的提示文案 | 让用户输入时显示的中文说明 |
| `=` 后的默认值 | 可选。有默认值 → 回车用默认；无默认值 → 必填，不能为空 |

**解析逻辑（安装器）：**

1. 在当前块内抓取所有 `^#PARAM_[a-zA-Z0-9_]+:…` 行
2. 以第一个 `:` 切开：左边是 `PARAM_KEY`，右边是 `描述[=默认]`
3. 再以第一个 `=` 切开描述与默认值
4. 交互：
   - 有默认：打印「默认值: xxx (直接回车使用默认值)」
   - 无默认：打印「(无默认值，必填，请自行设定)」，空输入会循环要求重填
5. 用 `sed` 把块内所有 `{PARAM_KEY}` 替换成用户输入值
6. 删掉所有控制行 `#START` / `#END` / `#NAME` / `#PARAM_*`，得到干净 YAML 服务块
7. 合并进本机 `/app/docker/docker-compose.yml`

### 3.3 占位符写法

在 compose 正文里用花括号引用参数：

```yaml
ports:
  - "{PARAM_PORT}:25774"
environment:
  - ADMIN_USERNAME={PARAM_ADMIN_USERNAME}
  - ADMIN_PASSWORD={PARAM_ADMIN_PASSWORD}
```

安装后变成真实值，例如：

```yaml
ports:
  - "25774:25774"
environment:
  - ADMIN_USERNAME=admin
  - ADMIN_PASSWORD=admin
```

---

## 4. 完整最小示例

```yaml
services:
#START
#NAME:探针监控komari
#PARAM_PORT:端口=25774
#PARAM_ADMIN_USERNAME:管理员用户名=admin
#PARAM_ADMIN_PASSWORD:管理员密码=admin
  komari:
    image: ghcr.io/komari-monitor/komari:latest
    container_name: komari
    restart: always
    ports:
      - "127.0.0.1:{PARAM_PORT}:25774"
    volumes:
      - ./komari:/app/data
    environment:
      - ADMIN_USERNAME={PARAM_ADMIN_USERNAME}
      - ADMIN_PASSWORD={PARAM_ADMIN_PASSWORD}
#END
```

**用户在面板看到的流程：**

1. 列表项：`探针监控komari`
2. 选「1 安装」
3. 依次提问：
   - 端口（默认 25774）
   - 管理员用户名（默认 admin）
   - 管理员密码（默认 admin）
4. 生成服务 `komari`，写入本机 compose，并 `docker compose up -d`

---

## 5. 多服务块（完整栈）

一个 `#START`…`#END` 里可以写多个服务，例如「CPA + CPAMP 完整栈」：

- `#NAME:CPA + CPAMP 完整栈`
- 多个 `#PARAM_*`（面板端口、网关端口、各类密钥）
- 服务键：`cli-proxy-api`、`cpa-manager-plus`

安装器行为：

- 提取块内所有 `  服务名:` 行作为服务键
- 若同时含 `cli-proxy-api` 与 `cpa-manager-plus`，强制先起 CPA 再起 CPAMP
- 本地无镜像时先 `pull`，再 `up`
- 安装成功后打印并保存初始化信息（地址 / 密钥）到  
  `/app/docker/cpa-manager-plus/INIT_INFO.txt`

**建议：** 能拆成单服务的尽量单服务一块（列表更清晰，卸载更干净）；强依赖栈再做成多服务一块。

---

## 6. 本机 compose 如何分区

安装器不会整文件覆盖，而是用分区标记保护其它应用：

```yaml
#TOOLBOX_BEGIN:<应用显示名>
  <服务 YAML>
#TOOLBOX_END:<应用显示名>
```

- **覆盖安装**：只替换本应用 `BEGIN/END` 分区
- **卸载**：删容器 + 清本分区（默认可保留挂载数据目录）
- **抹除**：删容器 + 删挂载数据 + 清本分区

数据目录约定：相对路径如 `./komari` 实际落在 `/app/docker/komari`（工作目录为 `DOCKER_ROOT=/app/docker`）。

---

## 7. 当前模板中的应用（以仓库文件为准）

| `#NAME` | 主要服务键 | 典型参数 |
|---------|------------|----------|
| 探针监控komari | `komari` | 端口、管理员用户名/密码 |
| npm服务 | `npm` | 端口 |
| PostgreSQL 18 | `postgres` | 端口、用户、密码、库名 |
| Redis 8.2 | `redis` | 端口、密码 |
| New API | `new-api` | 面板端口、SQL_DSN、REDIS_CONN、节点名、时区、流式超时 |
| CPA + CPAMP 完整栈 | `cli-proxy-api` + `cpa-manager-plus` | CPAMP/CPA 端口、Admin Key、Management Key、API Key |

列表以远端 `docker-compose-tools.yml` 实时内容为准；改完需 **commit + push**，本机可在百宝箱选 **98 强制刷新**。

---

## 8. 如何新增一个应用

1. 打开 `docker-compose-tools.yml`
2. 在文件末尾已有 `#END` 之后追加：

```yaml
#START
#NAME:我的应用显示名
#PARAM_PORT:端口=8080
#PARAM_TOKEN:访问令牌
  my-app:
    image: example/my-app:latest
    container_name: my-app
    restart: always
    ports:
      - "{PARAM_PORT}:8080"
    environment:
      - TOKEN={PARAM_TOKEN}
    volumes:
      - ./my-app/data:/data
#END
```

3. 注意：
   - 服务键缩进 2 空格（在 `services:` 下）
   - 占位符必须写成 `{PARAM_PORT}` 这种与标签一致的名字
   - 有合理默认值的写 `=默认`；密钥类可不写默认（强制用户输入）
   - 挂载建议用 `./服务名/...`，方便卸载/抹除对齐
4. push 到 `main` 后，服务器上进百宝箱或选 `98` 刷新

---

## 9. 有效性校验

安装器判定模板「可用」的条件（`_is_valid_tools_yml`）：

- 文件非空
- 至少有一行 `^#NAME:`
- 至少有一行 `^#START`
- 不是 GitHub 404 HTML / JSON 错误页

---

## 10. 与「旧说明草稿」的对应关系

仓库里曾有 `yml.txt`（已删除），原文含义即本文件正式版：

> 读取到 `#START` 就是一个容器（应用）的开头，直到 `#END` 是结尾；  
> 中间是配置；`#NAME` 是面板显示名；`#PARAM_` 开头是用户输入参数；  
> 例如 `#PARAM_ADMIN_USERNAME:管理员用户名` 表示提示用户输入管理员用户名，并写入 `{PARAM_ADMIN_USERNAME}` 位置。

本文件在此基础上补全了默认值语法、多服务栈、本机分区合并与新增步骤。
